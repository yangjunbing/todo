<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="projects.bing.dao.OrderDao">

    <!--新增 -->
    <insert id="add"  parameterType="projects.bing.entity.Orders">
        INSERT INTO orders t (id,wxid,total_price,state,notes,score,opinion,order_date,pay_date,
        payid,table,temp1,temp2,temp3)
        VALUES (#{id},#{wxid},#{totalPrice},#{state},#{notes},#{score},#{opinion},#{orderDate},#{payDate},
        #{payid},#{table},#{temp1},#{temp2},#{temp3})
    </insert>

    <!--更新 -->
    <update id="update"  parameterType="projects.bing.entity.Orders">
        UPDATE  orders  SET
          temp3 = #{temp3}
        WHERE id  =  #{id}
    </update>

    <!--删除-->
    <delete id="delete" parameterType="java.lang.String" >
        DELETE FROM orders WHERE  id = #{id}
    </delete>

    <!--查询一个 -->
    <select id="getOne" resultType="projects.bing.entity.Orders" parameterType="java.lang.String">
        select * from orders where id = #{id}
    </select>

    <!--查询全部 -->
    <select id="getAll" resultType="projects.bing.entity.Orders" parameterType="java.lang.String">
        select * from orders order by priority ASC
    </select>

</mapper>